//console.log(2 > 1); //two greater than one// its value always will come in boolean values/
//console.log(2 >= 1); //two greater than one or equal to one// its value always will come in boolean values/
//console.log(2 < 1); // tow less than one// its value always will come in boolean value.
//console.log(2 = 1); // two equal to one// its value always will come in boolean value/
//console.log(2 != 1); // two not equal two one// its value always will come in boolean value/

// most of the cause main hum below bale compariosn ko avoid karte hain is Tarha ke conversion kabhi bhi aapko confuseing mai daal sakte hain/

//console.log("2" > 1);
//console.log("02" > 1);

//console.log(null > 0);
//console.log(null == 0);
//console.log(null >= 0);

//strict check
// === // ye value ko strictly check karta hai// or ye dataType ko bhi check karta hai

console.log("2" === 2); // jab aap triple equl se check karenge to ye dataType bhi check karega // or ye conversion bhi nahin karega kyuki ye done same hain hi nahin/

